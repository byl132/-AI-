[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm install --production=false"]

[phases.build]
cmds = ["echo 'Build complete'"]

[phases.start]
cmd = "node server/server.js"

[variables]
PORT = "3000"
NODE_ENV = "production"
